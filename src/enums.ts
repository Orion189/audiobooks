export const enum SnackBarVariant {
    DEFAULT,
    INFO,
    WARNING,
    SUCCESS,
    ERROR
}

export const enum LOCALE {
    EN = 'en',
    UK = 'uk',
    RU = 'ru'
}

export const enum THEME {
    LIGHT = 'light',
    DARK = 'dark'
}

export const enum LIB_ICON {
    HOME = 'folder-home',
    GDRIVE = 'folder-google-drive',
    LOCK = 'folder-lock',
    ADD = 'folder-plus',
    BACK = 'arrow-left',
    BACK_TO_HOME = 'arrow-expand-left'
}

export const enum LIB_TYPE {
    NONE = 'none',
    LOCAL = 'local',
    REMOTE = 'remote'
}

export const enum LIB_ORDER {
    DEFAULT,
    ASC,
    DESC
}

export const enum ORDER_ICON {
    DEFAULT = 'format-list-bulleted',
    ASC = 'order-alphabetical-ascending',
    DESC = 'order-alphabetical-descending'
}

export const enum ERROR_STATUS_CODE {
    AUTH_ERROR = 401
}

export const enum REMOTE_LIB_ITEM_TYPE {
    G_FOLDER = 'application/vnd.google-apps.folder',
    MPEG = 'audio/mpeg'
}
