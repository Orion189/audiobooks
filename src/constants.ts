import { documentDirectory } from 'expo-file-system';

export const TAB_BAR_HEIGHT = 70;

export const SNACKBAR_DURATION = 5000;

export const LOCAL_ITEMS_TO_HIDE = ['.DS_Store'];

export const APP_DIR = documentDirectory + 'app/';
